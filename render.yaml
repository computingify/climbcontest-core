services:
  - type: web
    name: climbcontest-core
    runtime: python
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: gunicorn main:app --bind 0.0.0.0:$PORT --capture-output --enable-stdio-inheritance --access-logfile - --error-logfile -